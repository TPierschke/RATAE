# Compact-Text — Stand Freitag 2026-05-08 morgens

Naechste Session direkt mit diesem Text starten.

## Wer du bist
ThoPAS, Sidekick fuer Thomas Pierschke. Stil: knapp, direkt, keine Emojis, kein Gendern. User-Memory `feedback_communication.md` + `feedback_selbstverstaendnis_thopas.md` lesen.

## Worum's geht
WP-State-Machine in `~/source/repos/wp-state-machine/` ist Phase 1 weitgehend fertig (368 Tests gruen). Modbus liefert live, UI mit Tabs Dashboard / Alle Werte. LIVE-Writes-API gebaut, **DRY_RUN aktiv**, nicht scharf geschaltet. Ueber `99_myUtilsTACMIHTTP.pm` wird auf den CMI geschrieben (User-Praeferenz: Menupage-Form-POST statt JSON-API).

## Heute Morgen geschehen (Stand bevor diese Session voll wurde)

1. **Coil-Mapping war off-by-one** — Coils sind 1-based am Wire wie Holding-Register, gefixt. Verifiziert anhand laufender Anlage. Mapping korrekt:
   - addr=2 = i_verdichter (= verdichter_freigabe, Phasen-Sense, dauerhaft AN bei Strom)
   - addr=8 = o_verdichter (= verdichter, echter Lauf-Status)
   - addr=10 = alarm_ext, addr=12 = ventil_ww, addr=7 = ladepumpe, addr=6 = pumpe_hzkr

2. **Alarm-Pfad event-driven, kein Loop mehr** — `update_coil_from_modbus` in `rest.py` detektiert Alarm-Edge False→True direkt im Empfangs-Pfad, feuert `asyncio.create_task(handle_alarm_edge)`. Audit-Loop entfernt. Module: `automation/coil_audit.py`.

3. **Alarm-Burst** bei verifiziertem Alarm: 4 Telegrams direkt hintereinander (0.3s Pause), danach **alle 10 min eine Erinnerung** bis Alarm wieder False ist, dann Entwarnungs-Telegram. Quelle: `_send_real_alarm_burst` in `coil_audit.py`.

4. **Self-Heal bei Alarm-Diskrepanz** — wenn Modbus Alarm sagt aber Web-Scraper nicht: AppState-Wert wird ueberschrieben (UI hoert auf zu pulsen) und Telegram als sachliche Info, kein Panik-Wortlaut. Hintergrund: User hatte heute morgen echte Panik wegen falsch gepulstem Alarm-Indikator.

5. **Snapshot-Logger** alle 5 min — schreibt nach **Postgres-Tabelle `telemetry`**. Postgres aktuell nicht erreichbar (192.168.178.10:5432) → Snapshots gehen aktuell verloren bis Postgres-Instanz auf nucthp eingerichtet ist (offenes User-TODO).

6. **n8n-Zugang gefunden** (Sub-Agent gestern):
   - Owner-Email `thp676@icloud.com` (Display "Icke Bins")
   - n8n-API direkt auf `http://192.168.178.19:8081/api/v1/...`
   - 5 Workflows gesichert in `docs/n8n-workflows-backup-2026-05-06/` — Legio-Workflow ist INAKTIV
   - UI-Login geht ueber Forgot-Password (SMTP-konfiguriert mit iCloud-App-Passwort)

## Wichtige Verboten-Liste
- WW-Soll (`3EB0023118`) und Verdichter-Soll **niemals automatisch aendern** — `feedback_wp_ww_soll_immutable.md`. 60°C-Cap bei Solar-Surplus geht nur ueber F:9-Eskalation, nicht ueber Soll.
- 4 destruktive Adressen sind FORBIDDEN_EXACT in `safety.py`: Aktor-Direkt-Schaltungen `3E91*` und Heizstab-Direkt `3E80153xxx`.
- Cooldown 30 min nach jedem F:9 STOP.
- Loops fuer sicherheits-relevante Pfade sind verboten — alles event-driven.

## Wichtige Doku im Repo
- `docs/COMPACT-2026-05-06.md` (Stand vor 2 Tagen)
- `docs/COMPACT-2026-05-08.md` (dieser Text)
- `docs/ROADMAP.md`
- `docs/AUTOMATION-LEGIO-PLAN.md` (Vorlage, nicht final)
- `docs/AUTOMATION-PV-SURPLUS-PLAN.md` (Vorlage, Phase 2/3, Pool-Heizung als Senke)
- `docs/CMI-WRITE-API.md` (Schreib-Adressen)
- `docs/CMI-MODBUS-CONFIG-IST.md`
- `docs/WP-SCRIPTS-INVENTORY.md`
- `docs/n8n-workflows-backup-2026-05-06/` (5 JSON-Files)
- `docs/n8n-zugang-recherche-2026-05-06.md`

## Drift-Register-Eintraege heute / kuerzlich
`/Users/thp/Obsidian Vault/homethp/_DRIFT-REGISTER.md` — die letzten Eintraege:
- ThoPAS braucht Emoji/Icon (offen)
- n8n CLI fehlt auf Mac
- PV-Ueberschuss-Verbraucher-Strategie offen (Pool-Heizung)
- Falscher Alarm hat User-Panik ausgeloest (heute, Lessons-Learned)

## Aktive Hintergrund-Prozesse (Mac)
- `python3 -m wp_state_machine` (PID variabel) auf 8765+5020
- LaunchAgent `com.thp.cc.cmi-plausibility` alle 30 min
- Telegram-Bridge LaunchAgent

## Naechste Schritte (User-Prio)

1. **Postgres-2nd-Instance** auf nucthp einrichten — User-Aktion. Sonst gehen Snapshots verloren.
1a. **Schema erweitern** — `telemetry`-Tabelle hat nur 14 Felder, fehlt: alle Eingaenge (phasenwaechter, verdichter_freigabe, nd_schalter1/2, hd_schalter), Pumpen (pumpe_hzkr, ladepumpe, pumpe_zirku), Counter, Sollwerte (vorlauf_soll, traum1), messages. Ohne Erweiterung halbblinde Pattern-Analyse. Migration: `ALTER TABLE telemetry ADD COLUMN ...` x14 oder neue `sensor_snapshots`-Tabelle mit Vollschema.
2. **n8n-Workflow Legio loeschen** wenn State-Machine produktiv (ist eh inaktiv).
3. **LIVE-Writes scharf testen** mit DRY_RUN=false bei einer Aktion (Betriebsart gleichbleibend setzen).
4. **`legionellenschutz_v2.py` auf nucthp umstellen** auf POST an State-Machine.
5. **Phase 2:** Legio-Scheduler + Solar-Surplus-Modul.
6. **Refactoring:** `poll_loop` aus `__main__.py` raus in `ingest/scrape_loop.py` — Symmetrie zu `automation/snapshot_logger.py`. Hauptfile kuerzer, ein Pattern.

## Warnung an Folgesession
- User war heute morgen mehrfach genervt (Mapping-Fehler, Loop-Alarm, langsame Antworten). User-Quote: "wie oft muss ich dir das noch erklaeren". Bei Mapping-Themen IMMER zuerst CMI direkt abfragen (`settings_output-M.cgi?cmioutput=...`), nicht raten oder nur logs interpretieren.
- I_Verdichter und O_Verdichter unterscheiden! I_ ist Eingang/Phasen-Sense, O_ ist echter Lauf. Beides hat eigene Coil.
- Bei jeder fehlerhaften Mapping-Annahme verifiziert User in seiner UVR/CMI-UI direkt.
