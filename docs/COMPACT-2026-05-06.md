# Compact-Text — Stand 2026-05-06 abends

## Heute geschafft (chronologisch das Wichtigste)

- **Modbus als Primaerquelle live.** CMI sendet jetzt korrekt skalierte Werte an den Mac-Slave auf 5020. Aussen 10.4, Vorlauf 29.5, Ruecklauf 30.4, WW 50.x, Heissgas 40.x, Saugleitung 16.9 (auf 0.1 K genau), Counter Verdichter 13944 h / 24718 Schaltzyklen. `MODBUS_DROP_VALUES=False`.
- **CMI-Konfig repariert.** Kernbug war doppelt: 1) Holding-Register 1-based am Wire (outmag=N → addr=N+1), 2) CMI-internal in 1/100 °C statt 1/10 °C. Beides im Decoder korrigiert (`factor=0.01`, Map ab `1`). Plus: alte Geister-Konfig im Push-Thread des CMI per Reboot ueber Web-UI weggeraeumt.
- **UI komplett umgebaut.** Tabs Dashboard / Alle Werte. Verdichter-Dot oben gross. Sparklines fix Y-Axis. Webcrawler-Button auf Tab 2. FBH-Pumpe + Ladepumpe als Status-Dot (nicht Prozent — sind Zustaende). Aussen+WW oben, Vorlauf+Ruecklauf unten. Cache-Buster `?v=20260506-11`. FE-Version `0.1.5-fe-20260506-11` im Footer sichtbar.
- **Frontend-SSE robuster.** Initial-Comment + Heartbeats + sauberes Reconnect (kein Storm). Fallback-Polling /state alle 15s wenn SSE getrennt. Plus `/api/reset` mit Clear-Site-Data fuer alte Service-Worker.
- **LIVE-Write-Schicht gebaut.** `cmi_writer.py` mit allen 6 erlaubten Schreib-Adressen aus FHEM `99_myUtilsTACMIHTTP.pm`: Betriebsart, Normalsoll, Absenksoll, WW-Soll, WW-Boost Start/Stop. Whitelist erweitert (von 4 auf 6 Adressen). Schreib-Pfad geht durch `_execute_write` Helper: Whitelist → DRY/LIVE → Audit-Insert. **Aktuell DRY_RUN aktiv** — kein echter CMI-Schreibzugriff bis User explizit freigibt.
- **WP-Skripte inventarisiert.** Alle Hosts via SSH durchsucht. Konsolidiert in `docs/WP-SCRIPTS-INVENTORY.md`.

## Stand der State-Machine

- 368 Tests gruen
- Modbus-Slave laeuft auf 0.0.0.0:5020, FastAPI auf 0.0.0.0:8765
- DRY_RUN=True
- Postgres-Verbindung schlaegt fehl (192.168.178.10:5432 nicht erreichbar) — Telemetrie deaktiviert, alles andere laeuft
- Web-Scraper als Fallback wenn Modbus > 5 min stale
- Plausibility-Check-LaunchAgent laeuft alle 30 min

## Doku im Repo

- `docs/CMI-MODBUS-CONFIG-IST.md` — Soll-IST aller M-Outputs
- `docs/CMI-WRITE-API.md` — Schreib-Adressen mit Wertebereichen
- `docs/WP-SCRIPTS-INVENTORY.md` — Skript-Inventur ueber alle Hosts
- `docs/ROADMAP.md` — Phasenplan

## Tools

- `tools/cmi_read_modbus_outputs.py` — liest Konfig aller M1..M16 + M-1..M-16
- `tools/cmi_reset_modbus_outputs.py` — leert alle M-Slots (outmin=0)
- `tools/cmi_bulk_modbus_setup.py` — bulk-konfiguriert die 16 analogen
- `tools/cmi_bulk_modbus_digital.py` — bulk-konfiguriert die 16 digitalen
- `tools/plausibility_check.py` — laeuft als LaunchAgent (Modbus vs CMI-UI)

## Naechste Schritte (User entscheidet was zuerst)

### Kurzfristig (Phase 1 abschliessen)

1. **LIVE-Writes scharf schalten** — DRY_RUN=False testen mit gleicher Betriebsart wie aktuell (kein echter Wertwechsel, aber CMI-Call durchgefuehrt). Audit-Log-Zeile + CMI-Bestaetigung pruefen.
2. **`legionellenschutz_v2.py` umstellen** auf POST an State-Machine — Cron auf nucthp wieder aktivieren.
3. **Postgres-2nd-Instance** auf nucthp einrichten (User-Aktion).

### Phase 2 (nach Stabilisierung)

- **WW-Bereitungs-Erkennung** im `derive_state()`: wenn `ventil_ww=True UND verdichter=True` → State `WARMWASSER`. Plus eigenes Icon in der Haupt-State-Card.
- **SPS-Schema-View** mit Bilitas-Hebe-Schaltbild als Hintergrund. Skill `frontend-design` invoken fuer den optisch anspruchsvollen Teil.
- **Heizkurven-Auswertung** aus Postgres-Telemetrie ueber 1 Heizperiode.
- **Code-Review beinhart durch LLM-Council** (Grok + GPT + Mistral) inklusive aller destruktiven Schalter, Sicherheits-Whitelist, Race-Bedingungen — Ziel: Enterprise-Grade.

### Phase 3 (HA-Migration)

- `99_myUtilsTACMIHTTP.pm` umschreiben: Perl-Wrapper rufen statt direkt CMI die State-Machine.
- HA-Adapter mit eigener WW-Boost-Automation (Solar-Ueberschuss-getrieben).

## Drift-Items (offen, niedrige Prio)

- Heissgas-Wert wandert leicht (Modbus zeigt 37.x, CMI-UI 40.3). Vermutlich nur Push-Latenz, beobachten.
- Sensor-Offset-Korrektur an UVR (-4 K an E2/E3) — naechste Heizperiode.
- HA OS hat kein SSH aktiviert — fuer Phase 3 ueber HA-API arbeiten.

## Was nicht ich, was du

- CMI-Konfig: ich kann remote lesen/schreiben aber bei zweifelhaften Aenderungen darfst du gegenchecken via Web-UI.
- Postgres auf nucthp: User-Setup-Aufgabe.
- LIVE-Mode-Freigabe: nur auf explizite User-Anweisung `DRY_RUN=false`.

## Memory-relevante Updates fuer ThoPAS

- WP-State-Machine ist als zentrale CMI-Layer etabliert. Schreib-Pfad geht ueber `cmi_writer.py` mit Whitelist.
- DRY_RUN=True ist Default, **nie** ohne ausdrueckliche User-Freigabe deaktivieren.
- 4 destruktive Adressen (Aktor-Direkt-Schaltungen 3E91*, Heizstab-Direkt 3E80153xxx) sind explizit verboten und nicht in der State-Machine implementiert.
